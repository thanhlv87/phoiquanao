name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Cài đặt
        run: npm ci

      - name: Kiểm tra kiểu
        run: npx tsc --noEmit

      - name: Chạy test
        run: npm test

      - name: Build
        run: npm run build
